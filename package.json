{
  "name": "e-bookjs",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --ignore test/ -L src/server.js",
    "test": "cross-env NODE_ENV=test jest --watchAll --detectOpenHandles",
    "ci_test": "cross-env NODE_ENV=test jest --forceExit --detectOpenHandles",
    "migrate:make": "knex --cwd=src migrate:make",
    "migrate": "knex --cwd=src migrate:latest",
    "migrate:rollback": "knex --cwd=src migrate:rollback",
    "migrate:refresh": "npm run migrate:rollback && npm run migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "knex": "^2.4.1",
    "mysql": "^2.18.1",
    "path": "^0.12.7",
    "sqlite3": "^5.1.4"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.3.1",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.3"
  }
}
